# 発展的トピック

## 1. Hooks によるセキュリティ強化
サブエージェントは YOLO モードで動作するため、Hooks を使うとツール実行の前後にカスタムスクリプトを挟んでリスクを軽減できる。

## 2. MCP サーバー連携
MCP（Model Context Protocol）サーバーを使うと、Gemini CLI にカスタムツールを追加できる。

## 3. リモートエージェント（A2A プロトコル）
`kind: remote` を使うと、A2A プロトコルに対応した外部エージェントサービスをサブエージェントとして利用できる。
